#include <stdio.h>

int main() {
    int variavel_a = 0, variavel_b = 0, soma = 0;

    scanf("%d", &variavel_a);
    scanf("%d", &variavel_b);

    soma = variavel_a + variavel_b;

    printf("SOMA = %d\n", soma);
    return 0;
}