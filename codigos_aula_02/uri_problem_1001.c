#include <stdio.h>

int main() {
    int variavel_a = 0, variavel_b = 0, variavel_x = 0;

    scanf("%d", &variavel_a);
    scanf("%d", &variavel_b);

    variavel_x = variavel_a + variavel_b;

    printf("X = %d\n", variavel_x);

    return 0;
}