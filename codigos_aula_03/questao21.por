programa {
    /*
    Faca um programa para calcular n! (fatorial de n), sendo que o valor inteiro de n e
    fornecido pelo usuario.
    Sabe-se que:
    n! = 1 * 2 * 3 * ... * (n - 1) * n;
    0! = 1, por definicao.
    */
	funcao inicio() {
		
	}
}
